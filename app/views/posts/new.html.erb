<!-- View for creating a new `Post` -->
<!-- Basic rails form -->
<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :body %>
  <%= f.text_area :body %>
  
  <%= f.submit %>
<% end %>